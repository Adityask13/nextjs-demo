import styles from '../../styles/Home.module.css'


function About() {
    return (<section className={styles.main}>
        <p>Firebase: Used to get Listing page data hosted.
            Query individual product data with the use of Dynamic routing
        </p>
    </section>)
}

export default About